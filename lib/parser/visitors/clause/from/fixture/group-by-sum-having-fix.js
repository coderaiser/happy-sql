[
    select(
        dept,
        sum(
            salary,
        ),
        from(
            emp,
            groupBy(
                dept,
            ),
            having(
                sum(
                    salary,
                ) > 1000,
            ),
        ),
    ),
];
